services:
  - type: web
    name: deepnova
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    disk:
      name: deepnova-data
      mountPath: /opt/render/project/src
      sizeGB: 1
